A Pen created at CodePen.io. You can find this one at http://codepen.io/davilious/pen/YqdoyP.

 I just wanted to play a little with the different tools that Javascript has to work with arrays, `map()`, `some()`, `sort()` and `reduce()` are some of them.